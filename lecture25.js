//Export Default vs Named Export

//Named Export -> export multiple things with their name

//Default Export -> export one main thing from the file

export default function greet(name) {
    console.log(`Hello ${name}`)
}